# hoikushi_quiz プロジェクト

## 概要

保育士試験対策フラッシュカードWebアプリ。
静的HTMLサイトとして動作し、GitHub Pages でホスティングされている。
本番URL: https://hoikushi-quiz.com/flashcard/

## ディレクトリ構成

```
/
├── index.html          # リダイレクト（/flashcard/ へ転送）
├── script.js           # トップページ用スクリプト
├── style.css           # トップページ用スタイル
└── flashcard/
    ├── index.html      # アプリ本体HTML
    ├── app.js          # アプリロジック（クイズ・ストリーク・Supabase連携）
    ├── questions.js    # 問題データ（QUESTIONS 配列）
    ├── style.css       # アプリ用スタイル
    ├── privacy.html    # プライバシーポリシー
    ├── favicon.svg
    ├── ogp.png
    ├── hoiku-genri/        # 保育原理 科目別ページ
    ├── hoiku-shinrigaku/   # 保育心理学
    ├── jisshu-riron/       # 実習理論
    ├── kodomo-hoken/       # 子どもの保健
    ├── kodomo-katei-fukushi/ # 子ども家庭福祉
    ├── kyoiku-genri/       # 教育原理
    ├── shakai-fukushi/     # 社会福祉
    ├── shakaiteki-yogo/    # 社会的養護
    └── shokuji-eiyou/      # 食事・栄養
```

## 技術スタック

- **フロントエンド**: 素のHTML / CSS / JavaScript（フレームワークなし）
- **データ**: `questions.js` に問題データを直接定義（`QUESTIONS` 配列）
- **バックエンド**: Supabase（匿名クライアント、ランキング等の集計用）
- **ホスティング**: GitHub Pages
- **テスト**: なし

## 問題データの構造

`flashcard/questions.js` の `QUESTIONS` 配列に問題を追加する。

```js
{
  id: <number>,          // 一意のID（末尾の最大値+1）
  subject: "<科目名>",   // 例: "保育原理"
  question: "<問題文>",
  choices: ["選択肢A", "選択肢B", "選択肢C", "選択肢D"],
  answer: <number>,      // 正解のインデックス（0始まり）
  explanation: "<解説文>"
}
```

## 開発ルール

1. 変更前に何を変更するか説明してから実施すること
2. `questions.js` は巨大ファイル（500KB超）のため、編集時は対象箇所を絞って操作すること
3. Supabase の接続情報（URL・anon key）はパブリックキーなので公開リポジトリへのコミットは許容されている
4. デプロイはmainブランチへのpushで GitHub Pages が自動更新される
5. 科目別ディレクトリ（例: `hoiku-genri/`）内の `index.html` は科目フィルタ付きのエントリーポイント
6. 問題データの科目カバー状況を提案する際は、`grep` で必ず最新のカバー状況を確認してから提案すること。全9科目は常にカバーされている
7. mainブランチで直接作業した場合は、ユーザーに「マージ完了」「追加の作業は不要」と明確に伝え、実施状況をできるだけ簡潔に報告すること
8. SEO施策の実施内容は `CHANGELOG.md` に時系列で記録し、毎回マージの際にもファイルを更新すること
9. 複数ファイルへの大きな変更や構成変更を実装する場合は、説明だけでなく実際の位置をスクリーンショットやHTML構造図で視覚的に示してから実施すること
10. UIやコンテンツの構成変更を提案する際は、実装前に必ずビジュアル（イメージ図やスクリーンショット）を先に共有して、ユーザーの承認を得てから実装すること
11. 問題を追加したバッチごとに進捗表を作成する際は、実装後に必ず `grep -oE 'subject: "[^"]+"' flashcard/questions.js | sort | uniq -c` で実データと突き合わせ、カウント誤りを検出すること。進捗表の累計が誤ると以降すべてのバッチで累計ズレが生じるため注意
12. 各科目ページの `<script>window.PRESET_SUBJECT_COUNT = NN;</script>` はページ読み込み時に `app.js` が app-subtitle の表示を強制上書きするため、HTMLの数値だけ変更しても画面に反映されない。数値更新時は HTML・PRESET_SUBJECT_COUNT の両方を修正し、再帰検索で見落としを防ぐこと
13. mainへマージ後、GitHub Pagesへの反映には数分の遅延がある。変更が古い表示のままの場合はブラウザキャッシュが残っている可能性があるため、ユーザーにスーパーリロード（Ctrl+Shift+R / Cmd+Shift+R）を案内すること
14. 作業開始時に必ず `git fetch` で main との差分を確認すること。ローカルのファイル状態だけ見ていると、main で既に解消された問題を重複報告したり、古いデータで提案するリスクがある
15. CSS・JS の変更がHTML から反映されていない場合、ブラウザキャッシュ以前に **GitHub Pages の CDN（Fastly）キャッシュ** が問題になる可能性が高い。ブラウザでクエリ付きURL（`?v=1`など）を開いて確認し、検証なしに仮説で判断しないこと
16. CDN キャッシュ問題の恒久対策として、HTML内のCSS・JS読み込みURLにバージョン番号クエリを付ける：`<link href="../style.css?v=20260727">` など。更新のたびに番号を変えることで、CDNとブラウザ両方の新しいファイル取得を保証できる
17. 訴求カードなど繰り返し表示される要素に制限を実装する場合、ページ内に常設する要素（ページ構成の一部）と一時的な訴求（ユーザーアクション由来）を区別すること。前者は表示回数制限の対象外
18. sessionStorage が使えない環境（プライベートブラウジング等）に備え、失敗時はメモリ上のカウンタにフォールバックする実装にして、制限機能が無効化されないようにすること
19. GA4計測で複数箇所からの訴求をクリック測定する場合、`placement` パラメータだけでは各箇所が区別できない。`data-promo` と `data-placement` 属性で各訴求枠を明示的に識別し、計測の曖昧さを排除してから新規配置を追加すること
20. 施策の効果を左右する数字（ページ別トラフィック、検索需要、ユーザー行動データなど）が既に提供されている場合、設計・判断の前に必ず確認してから意思決定すること。手元にあるデータを見直さずに進めると、根拠のない判断につながるリスクがある
21. コンテンツの配置や導線設計時は、対象ユーザーの範囲を明確にすること。「特定科目の話題」と「試験全体に関わる話題」では適切な配置場所が大きく異なるため、概念的な整理が重要
22. GA4計測でカスタムパラメータ（`placement`, `promo_id`, `subject` など）を送っても、GA4側で「カスタムディメンション」として登録しないとレポートに表示されない。登録前のデータは遡及不可なため、実装後すぐに GA4 の「カスタム定義」で登録すること
23. 試験制度・出題数・配点・試験時間などの情報をサイトに記載する場合は、**学習者の不利益に直結するため、試験の実施機関（一般社団法人 全国保育士養成協議会）や所管省庁の公式発表など、確実な情報源に基づくこと**。特に教育原理と社会的養護は他科目と異なる制度設計のため要確認。確証が得られない場合は記載せず、公式情報の確認を促す注記に留める
24. 構造化データ（FAQPage等）の仕様は Google が定期的に変更する（例：2023年8月に FAQリッチリザルト表示が政府機関・医療系に限定）。実装前に必ず最新の仕様をGoogle公式ドキュメントで確認し、「期待される効果」を検証してから着手すること
25. 削減率・効果測定など数値を伴う提案は、着手前に必ず実測で検証してから進めること。推定値のまま実装すると根拠のない変更になり、ファイル可編集性を損失するリスクがある
26. 次の作業に着手する際に `git checkout -B <branch> origin/main` でブランチをリセットする場合、**未マージのPRが残っていないか必ず確認すること**。未マージのコミットはリセットで失われる。確認手順：`git log origin/main..HEAD` で main に未反映のコミットを洗い出し、残すべきものがあれば先にマージするか cherry-pick で引き継ぐ
27. 複数の科目ページに同一構造のコンテンツ（FAQ等）を横展開する場合、各科目固有の内容に踏み込まないと Googleから内容の薄い重複と判断される。共通テンプレートを避け、科目ごとの個別性を確保してから実装すること
28. 問題の「穴埋め」戦略として文字列マッチでキーワード出現数を測る場合、「完全一致」「緩い一致」「新規IDのみ」など複数の流派で大きく異なる結果が出る。単一の数え方を信頼せず、実装前に必ず複数の検証方法で一致を確認し、異なる結論が出た場合は根拠を明示してからキーワード追加を判定すること
29. 学習コンテンツの充足度判定は、キーワード出現数では測り切れない。単なる「穴探し」では限界があるため、問題追加の目的を「欠落キーワード補完」から「科目バランス調整」「出題形式の多様化」「難易度階層化」など複数基準に切り替えて総合判断すること
30. パフォーマンス改善の提案時、対策が問題の所在と実際に噛み合っているか入念に確認してから実装すること。科目分割のように「ファイルサイズは削減できても、実際に負荷が減る場所と無関係」な対策では効果がない。根拠があっても必ず実測で検証してから着手
31. スクリプト読み込みやデータ参照の流れを追跡する際、「ファイルサイズが大きい = 必ず初期表示に影響」という推定ではなく、実装上の参照タイミングと描画フロー（いつ・どこで・何のために読み込まれるか）を正確に把握してから判断すること
32. パフォーマンス改善策の効果判定は「手元での再現結果」だけでは不十分な場合がある。開発環境（フォント不在など）と本番環境が異なる場合、実装後の実測によって初めて効果が判明することを想定し、その旨を提案時に明記すること
33. `font-display` の値は「フォントの読み込み開始」を起点に判定される。ページ読み込み開始が起点ではないため、CSSを非同期化すると判定の起点が描画後にずれ込み、`optional` でも差し替え（＝CLS）を防げない。フォント切替由来のCLSを確実に0にするには、Webフォント自体を使わないか、代替フォントの字形を `size-adjust` 等で合わせ込むしかない
34. 計測ツールの結果が前回と完全一致した場合、それは改善が無かったのではなく「同じレポートが返っている」可能性を先に疑うこと。PageSpeed Insights は短時間の再測定に前回結果を返し、GitHub Pages のHTMLもCDNに約10分キャッシュされる。切り分けにはURLへクエリを付けて計測し、あわせて view-source で配信中のHTMLを直接確認すること
35. 外部サイト（公式ページなど）の情報を参照する必要がある場合、実行環境からアクセスできない場合は、アクセスブロック状況を説明した上でユーザーに原文の提供を依頼してから判断を進めること。アクセス不可のまま進めると、学習者の不利益に直結する可能性がある
36. 試験実施要領などの変更可能性が高い情報をサイトに掲載する場合、「この内容は〇〇年〇月〇日時点のもの」と基準日を明示し、実装後は GitHub Actions で年2回（2月1日・8月1日）に自動棚卸しIssueを作成する。変更がなくても基準日は更新する運用ルール（日付が古いままでは注記の意味が薄れるため）
37. 試験制度のコンテンツ実装後は、実装時のIssueやPRに「変更リスク評価」（低・中・高）を記載し、将来の棚卸しで確認すべき項目を明確にしておくこと
38. 試験要領など定期確認が必要な情報について、外部サイトがアクセス不可の場合は、毎回テキスト提供をもらうのではなく、棚卸しワークフローのIssueチェックリストにユーザーが公式ページと見比べる手順を組み込んでおくこと。これにより確認作業が確実かつ効率的になる
39. 法令引用や選択肢の正誤判定を含む問題について、複数の選択肢が同時に同一の条文要件を満たすリスクがある。修正・新規追加時は条文の正確な文言を確認してから実施し、選択肢が一意に決定されることを必ず検証すること
40. 無料学習サイトへの広告配置時は、単なるクリック率ではなく、サイト全体の信頼性と学習導線を優先して判断すること。「広告が先に目に入る構成」は避け、主要機能をすべて見終えた後の位置に配置するほうが、長期的なユーザー信頼につながる
41. 問題の正誤判定（ルール39の検証）時には、内容の正確性に加えて、選択肢の文字数が大きく異なる場合、「長い選択肢=正解」という受験テクニックとして知られているため、選択肢の長さを揃える等で知識なしに当てられない設計を確認すること
42. 問題文が「市町村が〜」と明記されている場合、「実施は都道府県の義務」という選択肢は内容知識なしに矛盾で消すことができる。出題の詭弁性をチェックし、知識を測る問題として機能しているか確認すること

## よく使うコマンド

```bash
# ローカルで確認（静的サーバが必要）
npx serve .

# 特定ファイルの問題数確認
grep -c '"id":' flashcard/questions.js
```
